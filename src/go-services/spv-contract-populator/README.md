# spv-contract-populator

## Install

  ```
  git clone github.com/distributed-lab/spv-contract-populator
  cd spv-contract-populator
  go build main.go
  export KV_VIPER_FILE=./config.yaml
  ./main run service
  ```

